name: flutter_tutorial_currency_converter_app
description: Currency converter application written in flutter and OOP clean architecture
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  connectivity: ^3.0.6
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  http: ^1.1.0
  shared_preferences: ^2.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.3
  mocktail: ^1.0.0
  bloc_test: ^9.1.4
flutter:
  uses-material-design: true